# TODO - Remove Sort Dropdown from Customer Page

## Plan Approved: Remove sort dropdown feature from Items.jsx

### Steps:
1. [x] Remove the `sortBy` state and its setter from Items.jsx
2. [x] Remove the `useEffect` that watches `sortBy`
3. [x] Remove the sort parameter from the API call
4. [x] Remove the client-side sorting logic (keep in-stock first sorting)
5. [x] Remove the sort dropdown UI from the sub-header

### File to Edit:
- `frontend/src/pages/Items.jsx`

## Completed!
The sort dropdown feature has been removed from the customer page (Items.jsx).
